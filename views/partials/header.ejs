<!DOCTYPE html>
<html>
    <head>
        <title>YelpCamp</title>
        <link rel="shortcut icon" type="image/x-icon" href="https://png.icons8.com/xbox-y/androidL/1600">
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
        <link rel="stylesheet" href="/stylesheets/main.css">
        <script src="https://use.fontawesome.com/50bb3a9917.js"></script>
    </head>
    <body>
        
    <nav class="navbar navbar-default">
        <div class="container-fluid">
            <div class="navbar-header">
                <a class="navbar-brand" href="/"><span class="glyphicon glyphicon-tent"></span> YelpCamp</a>
            </div>
            <div class="collaps navbar-collapse">
                <ul class="nav navbar-nav navbar-right">
                    <% if(!currentUser){ %>
                        <li><a href="/login"><i class="fa fa-user" aria-hidden="true"></i> Sign in</a></li>
                        <li><a href="/register"><i class="fa fa-user-plus" aria-hidden="true"></i> Sign up</a></li>
                    <% } else { %>
                        <li><a href="#">Signed in as <%=currentUser.username%></a></li>
                        <li><a href="/logout"><i class="fa fa-user-o" aria-hidden="true"></i> Sign out</a></li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>
    
    <div class="container">
        <% if(error && error.length > 0){ %>
            <div class="alert alert-danger" role="alert"><%= error%></div>
        <% } %>
        <% if(success && success.length > 0){ %>
            <div class="alert alert-success" role="alert"><%= success%></div>
        <% } %>
    </div>
    
